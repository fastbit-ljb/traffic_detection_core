[pytest]
asyncio_mode = auto
addopts = --ignore=tests/e2e --ignore=tests/load_test.py -m "not integration and not e2e and not legacy"
markers =
    integration: tests that require an external running service or model download
    e2e: browser tests that require a local frontend and browser driver
    legacy: tests retained for the pre-v2 service contract
